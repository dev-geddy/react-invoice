# @spec L2-INF-01, L2-INF-02, L2-INF-05, L2-INF-06, L2-INF-14
name: backflip

# Modes (profiles):
#   docker compose up -d                           → backflip-db only (native dev: corepack yarn dev on 3070)
#   docker compose --profile web up -d --build     → db + one-shot migrate + backflip-web on http://localhost:3071
#   docker compose --profile seed run --rm db-seed → one-shot owner seed (ADMIN_EMAIL/ADMIN_PASSWORD via .env.init or shell env)

x-db-url: &db-url postgres://${POSTGRES_USER:-backflip}:${POSTGRES_PASSWORD:-backflip_local_dev}@db:5432/${POSTGRES_DB:-backflip}

services:
  db:
    image: postgres:17-alpine
    container_name: backflip-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-backflip}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-backflip_local_dev}
      POSTGRES_DB: ${POSTGRES_DB:-backflip}
    ports:
      # Host port off the default 5432 to avoid clashing with other local pg.
      # Bound to loopback so the dev db with its default password isn't exposed
      # on the network if this file is ever run on a non-local host.
      - "127.0.0.1:${POSTGRES_PORT:-5544}:5432"
    volumes:
      - backflip_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-backflip}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # One-shot: applies committed drizzle migrations, then exits. Runs
  # automatically before backflip-web (and before the owner seed).
  db-migrate:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      target: deps
    container_name: backflip-db-migrate
    profiles: [web, seed]
    command: ["corepack", "yarn", "db:migrate"]
    environment:
      DATABASE_URL: *db-url
    restart: "no"
    depends_on:
      db:
        condition: service_healthy

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    container_name: backflip-web
    profiles: [web]
    restart: unless-stopped
    # AUTH_SECRET, AUTH_GOOGLE_* etc. come from the root env files.
    env_file:
      - path: .env
        required: false
      - path: .env.local
        required: false
    environment:
      # Inside the compose network the db is reachable at host `db`, port 5432
      # (overrides DATABASE_URL from env_file, which points at localhost).
      DATABASE_URL: *db-url
      # The dockerized app is reached on host port 3071, not the native 3070.
      AUTH_URL: http://localhost:3071
      AUTH_TRUST_HOST: "true"
    ports:
      # Host 3071 → container 3070 (same internal port as local dev).
      - "3071:3070"
    depends_on:
      db:
        condition: service_healthy
      db-migrate:
        condition: service_completed_successfully

  # One-shot owner seed (`corepack yarn init-owner`). Reads ADMIN_EMAIL /
  # ADMIN_PASSWORD from .env.init (env_file below); to pass them from the
  # shell instead: docker compose --profile seed run --rm -e ADMIN_EMAIL -e ADMIN_PASSWORD db-seed
  db-seed:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      target: deps
    container_name: backflip-db-seed
    profiles: [seed]
    command: ["corepack", "yarn", "init-owner"]
    env_file:
      - path: .env.init
        required: false
    environment:
      DATABASE_URL: *db-url
    restart: "no"
    depends_on:
      db:
        condition: service_healthy
      db-migrate:
        condition: service_completed_successfully

volumes:
  backflip_pgdata:
